[project]
name = "comfyui-apogalleria"
description = "Visual prompt-library manager for Ideogram 4 (ApoGalleria-Ideo4), plus sibling converters that turn its captions into Flux.2 structured JSON (ApoGalleria-Flux2) and natural-language prose for Z-Image Turbo/Krea2/Qwen-Image/Flux(1)/Flux2-NL-mode (ApoGalleria-NL)."
version = "1.0.0"
license = { file = "LICENSE" }

dependencies = [
    "Pillow>=9.0.0",
]

[tool.comfy]
PublisherId = "apoloniart"
DisplayName = "ApoGalleria"
Icon = ""
Tags = ["gallery", "ideogram", "flux2", "prompt-builder", "library", "utility", "converter"]
